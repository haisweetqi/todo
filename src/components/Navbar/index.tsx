import { Layout } from "antd";
import React from "react";
const { Header } = Layout;
const Navbar = () => {
  return <Header style={{ padding: 0, background: "#fff" }}>Navbar</Header>;
};

export default Navbar;
